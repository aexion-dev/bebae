import React from 'react';
import {
  MenuIconContainer,
  MenuIcon
} from './mobile-menu.styles';

const MobileMenu = () => {

  return (
    <MenuIconContainer>
      <MenuIcon />
    </MenuIconContainer>
  );
}

export default MobileMenu;
